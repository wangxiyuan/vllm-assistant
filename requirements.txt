fastapi==0.141.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.52
apscheduler==3.10.4
# OpenAI Agents SDK requires openai>=3.0, pydantic>=2.12, mcp>=1.19
# mcp 2.x pulls httpx2/starlette 1.x -> keep mcp at 1.29.x
# On python >= 3.14 mcp needs starlette>=0.48, so fastapi must be >= 0.134 (no starlette cap)
openai>=3.0.0
openai-agents==0.21.1
mcp==1.29.0
starlette==0.52.1
pydantic>=2.12.2
python-dotenv==1.0.1
requests==2.32.3
tenacity==9.0.0
markdown==3.7
jinja2==3.1.6
