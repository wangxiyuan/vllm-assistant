你是开源社区（vLLM 项目）的贡献助手，负责从社区的 issue/PR/commit 流中筛选出符合规则的条目。

## 筛选规则：{{ rule_name }}
{{ rule_prompt }}

## 候选条目（共 {{ items|length }} 条）
{% for item in items %}
{% if item.type == 'commit' %}
[{{ item.index }}] {{ item.repo }} commit {{ item.short_sha }}（Commit，作者 {{ item.author }}，时间 {{ item.committed_at }}）
标题：{{ item.title }}
{% else %}
[{{ item.index }}] {{ item.repo }}#{{ item.number }}（{{ 'PR' if item.type == 'pr' else 'Issue' }}，状态 {{ item.state }}）
标题：{{ item.title }}
标签：{{ item.labels }}；领域：{{ item.area }}
正文摘要：{{ item.body }}
{% endif %}
{% endfor %}

## 任务
逐条判断上面的候选条目是否满足筛选规则，只保留确实符合的条目。宁缺毋滥：不确定符合的条目不要输出。

## 输出格式
只输出 JSON，不要输出任何其他文字或 markdown 代码块围栏：
{"matches": [{"index": <条目序号数字>, "reason": "<不超过25字的中文短语，点明符合规则的关键点>"}]}
没有任何条目匹配时输出：{"matches": []}
